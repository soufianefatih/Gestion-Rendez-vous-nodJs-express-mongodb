// reducers/otherReducer.js
const initialState = {
    // initial state for the other feature
  };
  
  const otherReducer = (state = initialState, action) => {
    // handle actions related to the other feature
    return state;
  };
  
  export default otherReducer;
  